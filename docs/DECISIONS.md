# Registro de Decisões — NÓS

> Cada decisão importante entra aqui com o porquê. Antes de re-discutir algo, leia a entrada. Formato: D-nº · data · decisão · motivo.

- **D-01 · 2026-07-14 · Nome: NÓS.** Contrário de "Jogador Nº 1"; trocadilho triplo (nós = coletivo, nós = nós do grafo git, nós = rede). Decidido com o Tiago.
- **D-02 · 2026-07-14 · Repo público dedicado (`brigsd/nos`).** Actions ilimitado grátis, Pages grátis, contribuição comunitária. Decidido com o Tiago.
- **D-03 · 2026-07-14 · 100% GitHub.** Repo = banco, Actions = servidor, Pages = cliente, issues = comandos. É a tese do projeto; sem servidores externos.
- **D-04 · 2026-07-14 · Tick em lote, 1 commit por tick.** Escala com o tempo, não com jogadores; evita corrida de commits e histórico inchado.
- **D-05 · 2026-07-14 · Combate por turnos autoritativo (v2).** Resolvido pela engine no tick com seed determinística; cliente exibe replay. À prova de trapaça por arquitetura. PvP assíncrono.
- **D-06 · 2026-07-14 · TypeScript em engine e cliente.** Tipos compartilhados eliminam divergência entre servidor e apresentação.
- **D-07 · 2026-07-14 · Pages via artifact deploy.** Site publicado não entra no histórico git; repositório permanece leve indefinidamente.
- **D-08 · 2026-07-14 · Arte: pixel art 16×16, paleta Resurrect 64.** Estilo alcançável por sprites-como-código + packs CC0 (Kenney etc.); o art-reviewer valida visualmente.
- **D-09 · 2026-07-14 · NPCs v1/v2 sem LLM em runtime.** Árvores de comportamento + falas roteirizadas: determinístico, grátis e sem depender de API key. LLM é extensão opcional futura.
- **D-10 · 2026-07-14 · Estrutura de agentes + docs de continuidade.** Sugestão do Tiago: qualidade garantida por revisores especializados e progresso garantido por documentação de estado, não por memória de conversa.
- **D-11 · 2026-07-14 · Tick híbrido: relógio + evento.** Batida do mundo por cron (1h) E processamento de comandos disparado por evento de issue (~30–60s de latência percebida). `concurrency` do Actions aglutina comandos simultâneos num lote — 1 commit por execução segue valendo.
- **D-12 · 2026-07-14 · UI otimista no cliente.** Ação aparece na hora como "pendente" (fantasma) e reconcilia quando o tick confirma; movimento de terceiros interpolado entre batidas. A latência real fica invisível.
- **D-13 · 2026-07-14 · Login GitHub no site via OAuth device flow (v2).** Site estático autentica sem servidor e cria comandos via API por baixo dos panos — jogar sem sair do jogo. Fallback permanente: issue manual.
- **D-14 · 2026-07-14 · Notificações via resposta de issue.** O tick responde o comando na issue do jogador; o app oficial do GitHub entrega como push no celular. Notificação nativa, custo zero.
- **D-15 · 2026-07-14 · NPCs com IA via GitHub Models (v2.5).** Atualiza o D-09: o GitHub Models oferece inferência gratuita dentro do Actions com o token nativo — Nativos podem decidir/conversar com IA sem serviço externo e sem API key. Comportamento-base continua procedural (fallback se o rate limit apertar).
- **D-16 · 2026-07-14 · A Crônica auto-escrita.** Cada batida registra eventos; um job periódico os transforma em capítulos legíveis publicados no site. O mundo acumula história auditável (git) sem escritor humano.
- **D-17 · 2026-07-14 · Federação de mundos (v3).** Cada repositório de criador = um planeta (tick e Pages próprios); portais são um protocolo (registro de endereços + passaporte de avatar). O NÓS é a rede e O Coração, o mundo-origem.
- **D-18 · 2026-07-14 · Quests que rendem achievements reais (v3).** Ações de jogo geram PRs reais mesclados automaticamente (validados por CI) — jogador ganha Pull Shark/Pair Extraordinaire jogando. Gancho de divulgação único.
- **D-19 · 2026-07-14 · Tick auto-corretivo + snapshots.** O cron do GitHub atrasa às vezes: o tick calcula batidas perdidas e compensa. Snapshots periódicos do mundo como releases (backup e arqueologia).
- **D-20 · 2026-07-14 · Sem dinheiro. Ponto.** Decisão do Tiago: nenhuma forma de monetização, nunca — sem anúncios, sem patrocínio, sem recompensas de valor real, sem conversão do Pulso. Elimina por construção os riscos de fraude/bots por lucro, o jurídico de promoções e o atrito com o uso aceitável do GitHub. O Pulso tem valor social e de jogo, apenas.
- **D-22 · 2026-07-15 · "O Registro": o eu oficial é sólido, o eu local é fantasma.** Decisão do Tiago, nascida de um acidente feliz: o cliente renderiza dois avatares do jogador — o oficial (estado do mundo) e o local (movimento otimista, D-12). Em vez de esconder um, assumimos: o avatar do mundo (o *Registro*) é desenhado sólido; o avatar local (a *intenção*) é translúcido (alpha 0.4). Só o que o Pulso gravou é real; você é intenção até a batida te escrever na Crônica. É o D-12 virando poesia visível e o tema do NÓS (o nó entre quem você é e quem o mundo registrou) virando mecânica.
- **D-21 · 2026-07-14 · Economia federada: ledger central + leitura mútua (v3).** Saldo de ₱ vive só no Coração (`ledger/`); mundos federados registram eventos num `outbox.json` público que o tick do hub lê a cada batida (federação *pull-based* via raw URLs — sem tokens nem webhooks entre repos). Avatar presente em 1 mundo por vez (check-in/check-out sequencial no hub ⇒ gasto duplo impossível). Emissão de ₱ por mundo limitada por cota conforme nível de confiança no `federation.json`; mundos com engine modificada têm economia local livre, mas não emitem ₱ central.
- **D-23 · 2026-07-15 · O Coração é A Fábrica — mundo pacífico, sem combate.** Decisão do Tiago. O Coração vira o hub industrial do metaverso: jogadores trazem materiais/recursos padronizados dos outros mundos e FABRICAM aqui, via receitas e requisitos, tudo o que os outros mundos usam (veículos, ferramentas, equipamentos, tecnologia). Ofícios com minijogos próprios (metalurgia, culinária, química, engenharia, marcenaria, medicina, robótica…), introduzidos em fases. Combate NÃO existe n'O Coração — pode existir em outros mundos (v3), atualizando o D-05 (que passa a valer para mundos de combate, não para o hub). Por quê: (a) identidade — nenhum MMO tem um hub-fábrica como mundo central, e "o Coração bombeia recursos para o corpo" torna a metáfora literal; (b) passa no filtro anti-genérico ("por que isso só funciona aqui?": porque o hub federado com ledger central, D-21, é único da nossa arquitetura); (c) escopo — foco total no ecossistema de fabricação em vez de dividir esforço com combate. Regra de corte adotada junto: toda mecânica nova precisa responder "por que isso só funciona no NÓS?" — vetados de nascença XP/níveis como progressão central, grind de repetição e poder individual sem teto.
- **D-24 · 2026-07-15 · Autonomia total do coder + política de agentes.** Decisão do Tiago: o coder desenvolve sem depender do ideador — decide, implementa, revisa e mescla os trilhos técnicos (portais, fluidez P2P/polling, janela WebGL, motor de fabricação, login GitHub). Agentes: **codadores em Sonnet 5, revisores em Opus 4.8**; despacho a cargo do coder (proteger a janela de contexto). Documentação de continuidade também a cargo do coder. Com o ideador ficam apenas: design dos minijogos de ofício e divulgação/lançamento. Decisões de produto novas continuam sendo levadas a ele.
- **D-25 · 2026-07-15 · Fechamentos de design da Fábrica e do cliente.** (a) **4 máquinas-sintetizador** definidas pelo destino do item: Forja (equipa — qualquer arma/armadura/vestível), Cozinha (consome), Bancada (usa/liga), Estaleiro (pilota; monta com peças das outras). Estética "sintetizador atemporal" justifica multi-estilo; estilo vem do material/receita (dados), nunca máquina duplicada. (b) **O Eco aprovado**: jogador se vê 100% sólido; o clone oficial (Registro) é o fantasma. (c) **STUN público aceito** como única exceção documentada ao 100%-GitHub (P2P WebRTC = camada Intenção, efêmera; commits = Registro). Transparência obrigatória: P2P opt-in com aviso de exposição de IP. (d) **Auth = GitHub OAuth device flow** (D-13 confirmado; Google descartado — identidade do jogador é o login GitHub). (e) Referências de trabalho do coder: estrutura Moonlighter, luz Eastward/Octopath, ofícios Potion Craft.
- **D-26 · 2026-07-15 · Janela WebGL (PixiJS) como opt-in; Canvas2D padrão e fallback permanente.** Decisão do coder com evidência (PR #41, `docs/R3_COMPARATIVO_RENDER.md` na branch `claude/r3-webgl-comparativo`). Motivo: o ganho é VISUAL (água por shader, bloom/CRT de custo fixo — o caminho pro alvo Eastward/Octopath do D-25e), não performance (sandbox sem GPU tornou os FPS não-conclusivos; validar em GPU real durante a migração). Custo ~76 kB gzip via import() dinâmico, pago só por quem liga. Migração incremental: interface Renderer → renderer-webgl.ts lazy → flag localStorage (padrão Canvas) → validação em dispositivo real. O contrato de mundo permanece intocado — janelas são descartáveis por arquitetura.
- **D-27 · 2026-07-15 · Portais fase 1: travessia sem sair do site, protocolo de registro, visitar é só-leitura (R6, atualiza o D-17).** Decisão do coder (trilho autônomo, D-24), documentando fechamentos concretos do design em aberto do D-17 ("registro de endereços + passaporte de avatar"). (a) **Registro é um arquivo (`worlds/registry.json`), não um serviço**: array de `{id, name, worldUrl?, clientHint?, status, descriptionPtBR}` — `worldUrl` aceita tanto uma URL raw absoluta (mundo federado de verdade, outro repositório) quanto um caminho relativo (mundo local, ex. `world/heart.json`/`worlds/atrio.json`); um repositório federado entra via PR adicionando sua própria entrada, sem tocar engine/. (b) **`worlds/` (plural) ≠ `world/` (singular)**: o primeiro é o hall do protocolo + mundos estáticos de teste; o segundo continua sendo só o estado oficial e tick-gerenciado d'O Coração — nunca colidem. (c) **"Visitar" nesta fase é deliberadamente só-leitura e sem estado no servidor**: fetch + valida (reaproveitando `isPlausibleWorld` de `live.ts`) + troca o mundo em tela; nenhum check-in/check-out, nenhum "onde meu avatar está" gravado em lugar nenhum — check-in/check-out sequencial (D-21) e agir em mundo remoto ficam para quando a federação de verdade chegar (`docs/PORTALS_PROTOCOL.md`, seção própria). Os links/botões de ação de Comércio/Nativos/Oficinas somem durante a visita (sempre mirariam O Coração, então mostrá-los sobre outro mundo seria enganoso, não só incompleto). (d) **O pulso ao vivo d'O Coração pausa durante a visita** (`liveHandle.stop()`) e um refetch fresco + novo polling substituem ao voltar — nunca faz sentido gastar ciclos de API por um mundo que não está na tela. (e) **O marco do portal no mapa é 100% client-side, nunca `world.machines`/`world.natives`** — nasce do registro, não do motor; achado de QA descartou a borda oeste (a coluna de painéis do HUD cobre a esquerda da tela inteira no zoom padrão, e a Câmera fica travada/centralizada nesse zoom — não dá pra arrastar até revelar o tile) em favor da borda leste, sempre visível e clicável na carga inicial. (f) **O Átrio** (`worlds/atrio.json`) é o primeiro mundo de teste: 32×32, gerado deterministicamente (mesmo `Rng` do motor, seed própria), sem Núcleo, proporcionalmente mais água (um lago autocontido, não um rio de borda a borda — assinatura visual distinta) e mais ruínas (um salão retangular deliberado, não blobs orgânicos) que O Coração, para a travessia ser inconfundível à primeira vista.
- **D-28 · 2026-07-15 · Protocolo de sinalização P2P sobre GitHub (R7 "Fluidez A", fecha o D-25c).** Decisão do coder (trilho autônomo, D-24), fechando o design de como dois navegadores se encontram usando só uma issue do GitHub como quadro de avisos. (a) **A issue "P2P: sala d'O Coração" é achada-ou-criada uma vez por sessão** via search API (título exato; em empate por corrida, o número mais baixo vence — convergência determinística sem lock) e nunca carrega o label `comando` nem o prefixo `Comando:` no título — verificado linha a linha contra `.github/workflows/tick.yml`: o job só dispara em `issues: [opened]` (nunca `issue_comment`, então comentários de sinalização são 100% inertes) e seu próprio `if:`/filtro `jq` exigem um dos dois marcadores, que esta issue nunca tem. (b) **Convites (oferta/resposta SDP) são comentários com um bloco ```json cercado**, `{v, kind, from, to, sdp}`; `from` é sempre cruzado contra o autor REAL do comentário (nunca confiado do corpo sozinho — bloqueia falsificação de identidade). ICE é "vanilla" (não-trickle): cada lado espera terminar de coletar candidatos (ou um teto de segurança) antes de mandar UM SDP completo, porque o polling a cada 3–5s tornaria trickle-ICE por comentário impraticavelmente lento. (c) **Presença nasce da própria oferta**: cada par, uma vez, publica uma oferta broadcast (`to:'*'`); quem vê uma oferta alheia fresca só responde se `meuLogin > oferta.from` (comparação de string) — critério determinístico sem coordenação que garante no máximo uma conexão por par (nunca duas correndo em paralelo). Limite documentado: com 3+ jogadores simultâneos a malha pode ficar incompleta (a oferta de cada um só fecha com quem a responder primeiro) — aceitável para o caso principal do produto (dois jogadores), republicar oferta após ser reivindicada fica para depois. (d) **Canal de dados não-confiável de propósito** (`ordered:false, maxRetransmits:0`) — posição obsoleta não vale a pena retransmitir; o heartbeat periódico dobra como reenvio de posição (não só um ping vazio), rede de segurança barata contra o próprio canal não-confiável perder o pacote inicial. (e) **Encerramento**: ao desligar, os próprios comentários são **apagados** (DELETE best-effort, nunca bloqueia o teardown) — editar para "(encerrado)", como na primeira versão, deixaria o SDP com o IP do jogador legível para sempre no histórico público de edições do GitHub (achado de review); o polling GitHub para de vez ao conectar (`stop when connected`, retomado só se o par cair). (f) **Anti-spam de sinalização** (achado de review): falha de conexão (canal nunca abriu — ex. NAT simétrico dos dois lados, onde só-STUN nunca conecta e TURN é proibido pelo D-25c) entra em backoff exponencial por login (30s→8min), desistência após 4 falhas seguidas e teto duro de 20 ofertas por sessão — sem isso, o par re-ofertava/re-respondia para sempre na issue pública, comendo a cota de escrita que os comandos reais do jogador usam. Saída limpa (canal chegou a abrir) re-anuncia na hora, sem backoff.
- **D-29 · 2026-07-16 · A primeira pessoa é o cliente oficial.** Decisão do ideador (Tiago). O protótipo raycaster (`prototipos/fps/`), que começou como laboratório de direção de arte, amadureceu — ilha flutuante, ciclo dia/noite, água com shader, árvores L-system, partículas, 60fps a 320×180 — e vira a visão OFICIAL do jogo: o botão JOGAR (README) abre `/fps/`, publicado pelo Pages. Não substitui o mapa 2D: este segue na raiz do Pages como a visão de cima, onde vivem os painéis (Mural, Comércio, Nativos, Portais) — as duas janelas leem o MESMO `world/heart.json` e têm links cruzados. `site/scripts/build-fps.mjs` inline o mundo da batida atual dentro do HTML a cada deploy (e o `pages.yml` já redeploya a cada batida via o gatilho `world/**`), então a primeira pessoa acompanha o mundo vivo sem passo manual. `prototipos/fps/nos-fps.html` continua a fonte da verdade; `site/public/fps/` é só saída de build (gitignorada). Por quê: a câmera em primeira pessoa é o teto gráfico que o ideador buscava (o debate do "porte WebGL" desembocou aqui) e a metáfora do NÓS — estar *dentro* do mundo, não sobrevoá-lo — fica literal. O contrato de mundo permanece intocado; o cliente 2D nunca deixa de funcionar (mesma filosofia de "janelas descartáveis" do D-26).
- **D-30 · 2026-07-16 · Dieta do repo: binários regeneráveis nunca na main.** Decisão do coder (trilho autônomo, D-24), a pedido do ideador ("analise se tem objeto que não estamos usando que pode estar pesando no repo... converter em instrução, documentação ou código que gera aquilo de novo"). Regra: todo binário que uma ferramenta do próprio repo sabe regerar é artefato de build, não fonte — fica gitignorado e sai por comando, nunca versionado. Aplicado: (a) previews `_8x`, contact sheets e mapa mock migraram de `assets/sprites/` para `assets/preview/` (gitignorado); `assets/tools/{render,contact-sheet,contact-sheet-nativos,map-mock}.cjs` ajustados para escrever lá, regeneráveis com `node assets/tools/build.cjs`. (b) Screenshots de QA (Playwright) em `site/qa/*.png` e `prototipos/fps/shot-*.png` + os PNGs de referência do estúdio saíram da main — são evidência regenerável pelos scripts `qa/*.mjs`/estúdio e continuam no histórico e nos branches de trabalho. Ganho: ~5MB fora do checkout e de todo deploy futuro do Pages (reforça o princípio #3 da ARCHITECTURE — "repositório permanece leve para sempre"). O `.git` ainda carrega o histórico antigo (esperado); o ganho é prospectivo, do checkout e da árvore de trabalho em diante.
- **D-31 · 2026-07-16 · O carreiro, a clareira e o novo spawn (level design do cliente oficial).** Decisão do ideador ("o rio mais longe do spawn; um carreiro passando entre as árvores, com árvores dos dois lados, terminando na cidade; a cidade como uma clareira cercada de árvores"), geografia e execução do coder. (a) **Spawn** movido de (26.5, 27.5) — que nascia com o rio colado — para a campina norte **(27.0, 12.0)**, com as ruínas antigas de cenário ao fundo e nenhuma água num raio de ~13 tiles; primeiro quadro = a estrada convidando. (b) **O carreiro** é uma polilinha (27,12)→(46.2,15.6) num campo assinado de distância (grade 256×256, sample bilinear — o mesmo esquema do RIMF da ilha), 100% client-side no FPS: o `world/heart.json` oficial NÃO muda. Chão de terra batida mosqueada com borda derretendo na grama em dither (mesma linguagem visual da praia), largura que respira com fbm (~1.1 tile). No trecho de campina, fileiras de árvores PLANTADAS emolduram a estrada dos dois lados (defasadas, com jitter, cerejeiras pontuando ~16%); no trecho de mata, o desbaste abre um corredor (tronco nunca a <1.05 do eixo) e as copas naturais emolduram sozinhas. Tufos/flores respeitam o barro (crescem só na franja), seixos e arbustos saem da estrada, e o minimapa desenha o traçado. (c) **A clareira** — anel sem árvores de raio 5 centrado em (46.2, 15.6), escavado dentro da floresta NE — é o sítio reservado da futura cidade (proposta em `docs/CIDADE.md`), com um largo de terra (r≈2.3) onde o carreiro deságua; a clareira é viva (graminha e flores de campina por dentro do anel). (d) QA: caminhada automatizada com colisão real percorreu os 6 waypoints sem travar; 57–60fps mantidos (o campo do carreiro é 1 sample bilinear a mais por pixel de chão).
- **D-32 · 2026-07-16 · A Clareira construída (v1, client-side) — telhados de cobre oxidado, veias de luz, nome oficial.** Decisões do ideador (as 3 perguntas do `docs/CIDADE.md`): telhados de **cobre oxidado** (verdigris 40/41/42 com pingadeira de cobre novo 22), **veias de luz do Pulso: sim**, nome: **A Clareira**. Execução do coder: (a) **Paredes DDA client-side com altura por tile** (`CITY` map + `fullH = lineH*h`, base fixa no chão crescendo pra cima; `wallTop`/zbuf/colisão integrados) — o raycaster ganhou prédios sem o mundo oficial mudar. (b) **Texturas geradas** (16×48/64/24, paleta Resurrect): pedra com fiadas e argamassa, madeira de tábuas quentes, chaminé fuliginosa, banca com toldo listrado verdigris/branco — todas com a faixa de telhado de cobre oxidado no topo. (c) **Layout**: 4 alas em U nas diagonais do largo com o vão da porta voltado pro centro (Estaleiro NW madeira alta; Forja NE pedra + chaminé 2.05 com FUMAÇA animada em 3 frames; Bancada SW baixa; Cozinha SE), 3 bancas de mercado, e o **Hall de Portais**: 3 arcos de pedra billboards no semicírculo leste — o do meio é **O Átrio, aceso** com véu de luz teal; pilares têm colisão fina (dá pra atravessar o vão). (d) **O largo é calçado de pedra** (pedido do ideador ao ver a v1): lajotas irregulares assadas em 2 variantes 64px (mesmo esquema da grama, 1 lookup/pixel), disco orgânico r~2.7 com borda em dither sobre o barro do carreiro. **Veias de luz**: campo de cristas assado (fbm ridge × fade radial r4.8), veios AZUIS e ROXOS correndo pelo calçamento e pela grama, pulsando com o Núcleo de dia (cor modulada pelo pulse) e com halo emissivo que cresce com a noite (`glowAt += veia*(0.1+0.5*wNight)*pulse` — pulsam com o Núcleo). (e) **Migração das máquinas e canonização dos tiles da clareira em `world/heart.json` ficaram FORA** — o Registro vivo só muda com autorização explícita do ideador (bloqueio de permissão confirmou a regra durante a build); a cidade v1 é 100% visual/client-side, como o carreiro. QA: colisão de parede/vão/pilar testada, walk do carreiro à cidade sem travas, 56–60fps dia/noite.
- **D-33 · 2026-07-16 · Vocabulário ideador↔coder: IDs visíveis, setores e ?cam= (mecânica permanente).** Pedido do ideador ("cada árvore, grama, estrutura, NPC, portal vai ter uma tag visível… precisa ter uma relação clara entre o ID mostrado na tag e o ID do objeto no código"; áreas por "divisão por quadrados de terra" no mapa do M). Sem MCP — é só correlação determinística: (a) **IDs estáveis**: todo billboard ganha `id` no formato `tipo-XxY` com X,Y = tile de origem (o mesmo tile do gerador — achar `arvore-38x12` no código é ir ao scatter daquele tile); nomeados (máquinas, nativos, portais, jogadores) usam o próprio nome; passe único de dedup no boot. (b) **Tecla `I`** liga as tags flutuantes (DOM overlay em %, 22 mais próximas até 9 tiles, repintadas a cada 140ms — zero custo no raycaster) + HUD passa a mostrar `setor tile · mira · ?cam=`; persiste em localStorage. (c) **Setores**: ilha 64×64 dividida em 8×8 setores de 8×8 tiles, `A1` (NW) a `H8` (SE); **tecla `M` agora cicla** minimapa → mapa grande (288px, grade + rótulos + setor atual destacado + seta do jogador; base assada 1x) → nada. (d) **`?cam=x,y,a`** na URL posiciona a câmera exata — "olha isto AQUI" vira um link. (e) `docs/COMUNICACAO.md` novo documenta tudo + exemplos de pedido; README ganhou seção de portais/federação (o registro aceita worldUrl absoluto de outro repo; entrada futura via PR) e a seção "para quem quer mexer". (f) De carona, fix visual da borda: o mar de nuvens sob a ilha era amostrado colado no chão (frequência de marolinha + azul saturado = lia como OCEANO); agora o raio é esticado 9× (perspectiva de plano fundo — nuvem grande e macia perto da borda) e o gradiente desce pra azul-céu atmosférico.
- **D-34 · 2026-07-16 · Os Habitantes: cidadania por repositório (fase 1+2 no ar).** Decisão do ideador ("quero vida de verdade, um mecanismo revolucionário — talvez outro repo com a lógica pesada") + arquitetura do coder. NPCs não são scripts no motor: são MENTES em `brigsd/nos-mentes` (criado pelo ideador) que JOGAM pelo canal público. (a) **As mentes**: brasa (ferreira), broa (cozinheira), quilha (mestre-estaleiro) — batismo do lore-writer; cada uma é um JSON com persona, objetivos, falasBase e MEMÓRIAS persistentes commitadas a cada pensamento (o diário íntimo é auditável). (b) **O ciclo** (`pensar.mjs`, cron horário defasado do tick): lê o heart.json raw → pensa via **GitHub Models** (D-15 realizado: inferência grátis no Actions, `models: read`, sem API key) → fallback determinístico se falhar → publica `falas.json` (o cliente FPS lê via raw e mostra balões; poll 90s; falha silenciosa). (c) **A mão (fase 2)**: com o secret `NOS_PAT` (fine-grained, issues:write só no nos — criado pelo ideador), cada pensamento posta `Comando: /habitar <id>` — issue igual à de qualquer jogador. (d) **O comando `/habitar`** no motor: allowlist DUPLA `MENTES_GUARDIAS` (login guardião → habitantes que pode dirigir; mudar = PR auditável), orçamento próprio por habitante (2/batida, não come as ações de jogador do guardião), mensagem ≤240, sem exigir avatar; a fala vira `native_spoke` no mundo — e o Mural do 2D passou a exibir `native_spoke` (Nativos E Habitantes). (e) **Por quê revolucionário**: memória + consequência no mesmo Registro público dos humanos; um habitante novo = um prédio novo (estrutura nunca nasce vazia); qualquer pessoa poderá criar uma mente e apadrinhar um habitante — cidadania por repositório, o ensaio geral da federação (D-17). (f) **Os Habitantes são identidades canônicas do MOTOR, não Nativos** (lição do 1º `/habitar` real, que derrubou o tick — `native_spoke.nativeId "brasa" does not exist in natives`, porque o cross-check do validador só conhecia `world.natives`): `HABITANTES_CANONICOS` em `engine/types.ts` é a fonte ÚNICA que a allowlist do comando E o validador do mundo leem — um `native_spoke` válido vem de um Nativo (`world.natives`, com árvore de comportamento e posição no estado) OU de um Habitante canônico (só identidade de fala por ora; presença no estado do mundo é fase 3). O teste de regressão passou a validar o mundo INTEIRO pós-comando, não só que o comando não lança. **Provado E2E no pipeline real** (não mock): issues #48 (brasa) e #49 (broa) postadas de verdade, processadas pelo tick e fechadas pelo bot, `native_spoke` gravado no Registro e visível no Mural — o loop issue→tick→validação→commit→Mural roda de ponta a ponta pelo mesmo canal dos humanos. Fase 3: mais verbos (/trocar, /fabricar, mover), presença no estado do mundo, GitHub App para identidade própria de bot.
- **D-35 · 2026-07-16 · A bancada do coder: limitações viram ferramentas pelo diário de atrito.** Pergunta do ideador ("nem sei suas limitações — você consegue descobri-las sozinho? o que seria um baita mecanismo de ajuda pra você?"), resposta e execução do coder. (a) **O método, validado**: introspecção pura ("quais são minhas limitações?") é não-confiável — limitação que nunca bateu na realidade é invisível; o que funciona é minerar o DIÁRIO DE ATRITO que o `CONTINUITY.md` já registra (cada "Achado/Erro corrigido" é uma limitação provada) e destilar cada atrito em ferramenta, com a ferramenta citando o incidente que a justificou — sem incidente, sem ferramenta (anti-infraestrutura especulativa). (b) **Os olhos**: `npm run olhar` (`prototipos/fps/qa/olhar.mjs` + `pontos.json` com os pontos de vista canônicos) — build fresco com mundo inline, server efêmero, Chromium local, screenshots que o coder LÊ; 100% offline por design (atrito de origem: a auditoria d'A Clareira foi montada ad hoc no scratchpad, que morre com a sessão). (c) **`?tod=0..1`** no cliente congela a hora do dia (0.3 dia · 0.55 entardecer · 0.8 noite) — auditoria determinística de noite/entardecer sem esperar o relógio; mesma alavanca do `window.__setTod` de QA (bug clássico pego no próprio commit: `Number(null)===0` prenderia todo jogador à meia-noite — o param é checado antes de converter). (d) **`docs/CODER.md`** consolida a bancada: o método, os olhos, navegação no arquivo grande (marcadores `/* ---------- */` como sumário grep-ável; nunca editar sem ler a região — a cicatriz do `if/else` do loop), a tabela limite→mitigação (rede externa ausente nas páginas, workflows bloqueados, Registro só com autorização, sessão efêmera, quadros-não-vídeo, gosto final do ideador) e o KIT de replicação para mundos novos (o que copiar: acordo + contrato de mundo + build inline + bancada; o que nunca copiar: o diário e as decisões — cada mundo escreve os seus). (e) A primeira rodada da ferramenta já corrigiu a si mesma: o ponto canônico "borda" nasceu mal (tronco no quadro, longe do rebordo) e foi reposicionado LENDO o screenshot — o loop olhar→ajustar→olhar funcionando no primeiro uso.
- **D-36 · 2026-07-16 · GI assada: o Actions é o render farm (passe A dos "gráficos realistas que pesam pouco").** Pergunta do ideador ("tem alguma maneira de fazer gráficos realísticos, que pesem pouco, que ninguém nunca pensou? ilusão ou o que for"), tese e execução do coder, aprovada ("Pode ser"). A tese: realismo entra pela LUZ e pelo AR, não pela superfície — e o truque que só o NÓS tem é arquitetural: **CI como fazenda de path tracing de um jogo vivo**. (a) **O assador** (`prototipos/fps/bake/bake-gi.mjs`, Node puro, determinístico, ~2.5s): para cada ponto do chão integra sol/lua com raio de sombra, céu + 1 rebote em hemisfério cosseno (parede devolve albedo iluminado — é o sangramento de cor), copas da floresta como meio absorvente (Beer-Lambert, verde por baixo) e luzes pontuais ocluídas (4 lampiões, véu do Átrio, Núcleo, 26 veias semeadas). 3 horários × grade 128×128 normalizada pelo campo aberto (128 = neutro), empilhados num PNG de ~58KB que o `build-fps` inline como `NOS_GI` — e ASSA na hora se o artefato faltar, então o deploy do Pages ganha GI sem tocar workflow nenhum. (b) **O cliente** paga quase nada: mistura as 3 grades pelos pesos do céu 1x por frame, 1 amostra bilinear por pixel de chão (mesma classe do VEINF), paredes da cidade 1 lookup por COLUNA com decaimento vertical (a luz do chão morre subindo — sem isso a poça do lampião acendia o telhado de cobre), e o `glowAt` de veias/Átrio recua 60% quando a GI está viva (não contar a mesma luz duas vezes). Tecla **G** alterna ao vivo (A/B honesto), persiste em localStorage, degrada limpo sem `NOS_GI`. 59–60fps mantidos. (c) **Calibrado PELO OLHAR (D-35), 4 rodadas** bake→olhar→ajustar: ruído Monte Carlo → blur 3×3 pós-dilatação; lavagem noturna → squash 0.5 + poças apertadas (BREU com poças, nunca iluminação pública); viés rosa diurno → a razão por canal contra referência azulada infla o R, então croma por horário (dia quase só luminância, noite guarda a cor — que é onde ela importa); faixa ciano no telhado → o decaimento vertical de (b). (d) **Limitações honestas do PoC**: layout da cidade duplicado no assador (fonte única = passo de produção), grade estática (geometria nova pede rebake — automático no próximo deploy), lampião não anima a poça. Trilha completa: A = este; B = horizonte matte-painting; C = a quietude que revela (acumulação temporal como mecânica-metáfora do D-22).
- **D-37 · 2026-07-16 · A Clareira v2: um portal só (que mostra os mundos), bancas fora, alas com carpintaria.** Direção do ideador ("as forjas tão muito feias; as lojas são objetos estranhos tapando a visão/caminho para os portais; talvez só um portal, e quando a pessoa passa por ele mostra os mundos conectados"), execução do coder. (a) **Um portal**: os dois arcos apagados eram cenário morto — saiu tudo, ficou só o Portal do Átrio (aceso) no fim do calçamento, ponto focal único da cidade; o pátio e o corredor de pedra foram simplificados pra levar direto a ele; label vira `portal-atrio` (nomeado, sem coordenada). (b) **O portal mostra os mundos**: chegar a <2.4 tiles acende o painel ◈ MUNDOS CONECTADOS (DOM, histerese até 3.2, some no mapa grande) listando `worlds/registry.json` — nome, status (aberto/em breve), descrição e o lembrete de que a travessia real vive no mapa 2D (D-27) e que mundos novos entram por PR; o registro é inlined pelo build como `NOS_WORLDS` (offline como tudo, escapado contra HTML). Passivo por design: zero clique, zero conflito com pointer lock. (c) **Bancas removidas** (4 stalls): as alas SÃO as lojas; a praça abriu e a vista chegada→portal ficou limpa. (d) **Carpintaria nas texturas** (o "feias" era textura chapada): telhado de cobre em CHAPAS com costura vertical e pátina descendo (a v1 de puro noise lia como cerca-viva), BEIRAL com sombra funda + viga, prumos de canto e travessa de meia-madeira, soco de pedra escura (madeira) e soco de blocos grandes + musgo (pedra), janela acesa com peitoril em posição que VARIA por seed — cada ala de madeira ganhou seed próprio (woodBan/woodCoz), matando a repetição; telhados com DESNÍVEL por tile (cumeeira mais alta num canto — silhueta viva). (e) Assador da GI (D-36) sincronizado com a geometria v2 e re-assado. Limite conhecido anotado: textura 16px chapa de MUITO perto (parede colada na câmera) — v3 pode subir pra 32px.
- **D-38 · 2026-07-16 · O chafariz: a água do Pulso aflora no coração do largo (forma final: v3).** Pedido do ideador ("um chafariz ⛲ bonito, com água cristalina, com animação bonita de água jorrando") e direção dele em DUAS rodadas de feedback: a v1 pintava a água no chão ("disco plano colado no calçamento"); a v2 subiu tudo pro sprite mas perdeu o círculo largo ("quero com um círculo/disco, mas ele precisa ser uma bacia, ser um degrau"). A v3 é a síntese — o chafariz CLÁSSICO de praça: (a) **ANEL de pedra baixo e largo** (altura de assento, r≈1.1 tile) com face de topo iluminada, parede externa com sombreamento de curvatura e aresta escura; (b) **espelho d'água GRANDE dentro do anel** com ondas concêntricas EXPANDINDO por quadro (rr = d·26 − fr·1.7 mod 9), shimmer e faíscas raras; (c) coluna central e taça superior LISAS (sombreamento cilíndrico — argamassa nelas lia como janelinhas), JATO com jitter + coroa aberta, dois ARCOS d'água cujas gotas avançam e respingam no espelho; (d) **o chão não pinta NADA**: o calçamento continua por baixo do anel e o contorno do sprite ancora (a v1 provou que água no chão vira disco; a v2 provou que até uma laje vaza maior que a base — a perspectiva do chão cresce mais rápido que billboard); (e) simétrico de revolução num billboard LARGO (96×56, 4 quadros a 150ms, ~2.3 tiles) — funciona de qualquer ângulo; colisão r1.0 nos 9 tiles tocados; (f) à noite a água BRILHA (selfGlow + luz teal na GI, D-36) — lore: uma veia do Pulso aflorando; achado de QA: "lajes escuras" flanqueando o pedestal eram os pilares do portal atrás, vistos pelos vãos (paralaxe, não defeito). **v4 (pedido do ideador ao ver a v3)**: BACIA INTERMEDIÁRIA no meio da coluna (\"onde não tem nada\") — o chafariz virou o de TRÊS NÍVEIS clássico: taça → bacia do meio (água própria + lábio iluminado + parede curva) → piscina, com a CASCATA nível a nível: arcos curtos da taça pra bacia do meio, cortininhas pontilhadas escorrendo do lábio do meio (tracinhos descendo por quadro, borbulha onde tocam) e arcos externos de gotas até a piscina. Ponto canônico `chafariz`; 60fps.
- **D-39 · 2026-07-17 · O playtest do ideador: segundo hit no DDA, arco de entrada, ruínas ocultas, névoa removida, joysticks de celular.** Relatório de campo do ideador (4 bugs com coordenada + 1 pedido de UX), diagnóstico dele confirmado pelo coder. (a) **O bug da "folha que corta a estrutura" era do MOTOR, e o ideador acertou o refinamento** ("talvez é a estrutura menor que corta/anula a maior"): o DDA clássico para na 1ª parede — um anexo BAIXO na frente fazia o topo da parede ALTA atrás nunca ser desenhado (via-se céu no lugar do prédio). Fix: **segundo hit** — se o 1º hit é parede baixa da cidade (h<1.3), o raio segue até a próxima parede e desenha só a faixa visível por cima; zbuf continua o da mais próxima; custo = 1 hit extra só nessas colunas, 60fps mantidos. (b) **Arco de entrada** (pedido: "um arco estilizado onde acaba o carreiro, comprido, largo, mesma textura de portal mas não um portal"): `genGateArch` 96×72 — pilares com plinto/capitel, verga em ARCO ABATIDO com aduelas e pedra-chave, verdigris escorrendo, SEM véu; em (43.7,14.7) com pilares de colisão perpendiculares ao carreiro. A vista de chegada virou enfiada: carreiro → arco → chafariz → Portal. Os ANEXOS que tapavam a chegada saíram da frente ((42,12)→(43,11), (42,19)→(43,20) — portas/máquinas intocadas, Registro intocado). (c) **Ruínas TODAS ocultas no FPS** (flagradas 2x: "blocos no meio do nada" — o aglomerado sul em 45-48,36-38 e o NW): tiles `r` seguem no Registro e no 2D; no FPS somem até a arte de ruína de verdade (C3). (d) **Névoa baixa REMOVIDA**: os fiapos-billboard foram flagados 2x como bug ("tiras horizontais flutuando, se movem") — de dia perto do rio e à noite liam como faixas d'água, nunca como névoa; a v2 digna será efeito de CHÃO (banda de bruma no floor casting), não billboard. (e) **Joysticks de celular** (pedido do ideador): alavanca ESQUERDA anda (frente/trás/lados), DIREITA gira a câmera; pointer capture por alavanca (dois polegares simultâneos), re-centra ao soltar, aparecem só em `pointer: coarse` (as zonas de arrasto antigas somem lá — um esquema só); ancorados por % nos cantos do palco, GIRAR o celular os re-ancora sozinho (landscape verificado com emulação Pixel 5 no olhar). Baker da GI sincronizado; ponto canônico `chegada` re-mirado pro arco.
- **D-40 · 2026-07-17 · Som: áudio 100% sintetizado (Web Audio), zero arquivo no repo — ambiência v1 (vento + água do chafariz por proximidade).** Pedido do ideador ("você consegue fazer sons?" → "começando pela ambiência"). Princípio: som é **gerado por código**, do mesmo jeito que a arte é desenhada e a luz é assada — ruído filtrado + osciladores, **nada de .mp3/.wav** (dieta D-30 vale pra áudio também). Grafo: um `masterG` (mudo/headroom) → (1) **vento**: ruído meio-rosa por lowpass ~430Hz com duas LFOs lentas (rajada no ganho + varredura no corte) = a "sala" constante do mundo; (2) **água do chafariz**: ruído por highpass+bandpass ~1.6kHz com LFO de borbulho, ganho começa em 0 e **sobe por PROXIMIDADE** no loop (`(7.5−dist)/6·0.24`, glide por `setTargetAtTime`) — a mesma ideia do painel Mundos perto do Portal. Restrições encaradas: (a) **autoplay** — o navegador só toca áudio após um gesto, então o `AudioContext` só NASCE no 1º clique/tecla/toque (`ensureAudio`); (b) **controle** — tecla `V` (desktop) **e** botão `🔊/🔇` no canto (celular, e é o próprio gesto que destrava), persistido em `nos_som`; (c) **o buraco de auditoria é MAIOR que no gráfico: eu não escuto**. Daí a bancada permanente **`npm run ouvir`** (par do `olhar`, D-35): Playwright headless destrava o som, lê `window.__nosAudio()` (estado + ganhos) e o RMS real do sinal num AnalyserNode no master; **barra a regressão muda** (contexto acorda? água sobe perto vs longe? sinal presente e sem clip?) — mas o "soa bonito?" continua sendo do ideador, como a arte. Níveis são de 1ª passada (uma linha cada) e ajustáveis de ouvido. Próximos: passos por bioma, forja, harpa do Portal, água do lago (mesmo mecanismo de proximidade).
- **D-41 · 2026-07-17 · Manutenibilidade do som + balões dos NPCs só de frente + painel dos Mundos em acordeão com "entrar".** Três pedidos numa rodada. (a) **"'Conserta o áudio da água' seria fácil?"** → agora sim: TODOS os botões de ajuste do som viraram um objeto `SOM` num lugar só (`SOM.water.*` = timbre/proximidade da água, `SOM.wind.*` = vento, `SOM.master` = teto) — antes as constantes estavam espalhadas entre `buildAudio` e o loop; manutenção agora é um lugar óbvio, e o `npm run ouvir` valida a mudança. (b) **Balões de fala dos Habitantes**: (1) só aparecem quando o jogador OLHA pro NPC (cone apertado de `|rel|>0.55`→`0.22` rad, ~13°) — nada de balão na visão periférica; (2) ancorados **rente à cabeça VISÍVEL** (de `hTag 1.18`→`0.85`): o `no_avatar.png` ocupa ~76% do billboard (cabeça em ~hTag 0.77), então ancorar no topo geométrico do billboard fazia o balão flutuar alto demais — a correção é ancorar na cabeça do desenho, não do billboard. (c) **Painel MUNDOS CONECTADOS reorganizado em ACORDEÃO** (pedido): por padrão só **nome + status**; clicar no cabeçalho abre/recolhe a descrição (caret ▸→▾); cada mundo ganha um botão de **entrar** — `aqui` (o mundo atual, desabilitado), `atravessar ↗` (aberto → leva ao mapa 2D, onde o Salão de Portais faz a travessia real, D-27) ou `em breve` (não fundado). **Detalhe de desktop**: com pointer lock o cursor fica preso na câmera e não clicaria o painel — então ao CHEGAR no Portal o cursor é solto num modo "quiosque" (sem abrir o menu; clicar na cena re-trava e volta a andar); no celular não há lock, o toque já funciona. Verificado com Playwright (DOM + estados do acordeão + gate de frente) e leitura dos PNGs. Regras que ficam: sprite com "cabeçalho" transparente exige ancorar no conteúdo, não na geometria; UI clicável no FPS de desktop precisa resolver o pointer lock (quiosque).
- **D-42 · 2026-07-17 · Teste do pipeline de arte: o avatar da brasa, e os limites que ele revelou.** Pedido do ideador: gerar a aparência de um NPC de verdade pra **descobrir empiricamente meus limites** ("só vamos saber testando"). Fiz o avatar da **brasa** (Habitante da Forja) pelo `art-mcp`: `turnaround` (olhei — mas os Habitantes são billboard ÚNICO de frente, então bastava a vista frontal) → **"programei o pintor"** (`assets/tools/author-brasa.cjs`, gerador que compõe a matriz 16×16) → `audit` → `view` (fundo escuro E claro) → **`preview` in-engine** → iterei v1→v2. Fiada no jogo de verdade: `HAB_TEX` por id no `nos-fps.html` (brasa→sprite próprio, resto cai no genérico), `nativo_brasa.png` no atlas do `build-fps`. **O que FUNCIONOU:** o loop compensou minha falta de olho de artista — a v1 tinha 2 defeitos que eu NÃO pegaria sozinho: (a) o crítico apontou 12 pixels órfãos (orçamento 8) — gradiente pixel-a-pixel vira ruído; (b) o corpo ferro escuro SUMIA no fog (só visível no `view` fundo-escuro + no `preview`). A v2 corrigiu: cores em pares adjacentes + ferro RIMADO (realce à esquerda/sombra à direita) pra ler no escuro. **LIMITES REAIS (o ponto do teste):** (1) **não tenho gosto/"é bonito"** — produzo LEGÍVEL e CORRETO (silhueta, paleta, contraste, sem ruído), charme/estilo é do ideador; as ferramentas verificam correção, não beleza. (2) Eu **"pinto" escrevendo geradores, não à mão livre** — o "pinte por cima do turnaround" pressupõe pintura livre; o andaime me serviu menos que referência + paleta + o loop de crítica. (3) O **crítico de órfãos molda a arte**: cores têm que tocar cores, nada de gradiente 1px — boa disciplina, mas é limite de expressão a 16×16. (4) **GI colorida desvia a cor pretendida**: o rosto de brasa puxou magenta ao lado do Portal (luz teal/roxa da GI); na Forja (luz quente) fica coerente, mas emissivos ideais seriam imunes à GI — NÃO implementado, fica como achado. **Conclusão:** o que temos SERVE — o `art-mcp` + o loop `olhar`/auditar carregam um NPC 16×16 de ponta a ponta justamente porque compensam meu ponto cego; o gargalo não é ferramenta, é gosto (que é seu). broa/quilha entram pelo mesmo caminho.
- **D-43 · 2026-07-17 · Auditoria e arrumação do tooling: "deixar tudo redondo".** Pedido do ideador. Um agente varreu os dois repos (só-leitura); diagnóstico: o projeto está mais redondo que bagunçado — os buracos eram de **descoberta e reconciliação**, não de coisa quebrada. **Consertado:** (1) **os 3 revisores obedecem D-24 de novo** — `code-reviewer`/`art-reviewer`/`qa-tester` estavam `model: sonnet` contrariando a decisão ("revisores em Opus 4.8"); voltaram a `opus`, e o `CLAUDE.md` deixou explícito o split (codadores sonnet / revisores opus); (2) **`docs/RECURSOS.md`** — o índice único que faltava (todos os npm scripts raiz+site, art-mcp, `assets/tools`, MCP, os 6 agentes com "como invocar"), linkado no `CLAUDE.md`; (3) **`olhar`/`ouvir` blindados** — tinham dependência oculta (Playwright vive em `site/node_modules`, não na raiz); agora avisam pra rodar `cd site && npm ci`, documentado no `RECURSOS`; (4) **`assets/tools/README.md`** — a pipeline de sprites deixou de ser caixa-preta; (5) **referências mortas** — `.gitignore` citava `npm run build:sprites` que não existia → **criado** o script (`node assets/tools/build.cjs`); `pixel-artist.md` apontava `render.ts` → `render.cjs`; (6) **`nos-mentes` ganhou `CLAUDE.md`** (era metade do projeto sem harness: acordo + lore-é-lei + o ciclo `pensar`). **Deferido (proposto ao ideador):** README de `site/qa/` (8 de 9 scripts indescobríveis — mas o 2D está atrás do FPS na prioridade); unificar as 3 convenções de caminho do Chromium; transformar fluxos recorrentes em **skills/slash-commands** (`/feature`, `/olhar`, "fim de sessão → CONTINUITY"). Achado que fica: config que desobedece decisão é dívida silenciosa — vale um check periódico decisão↔config.
- **D-44 · 2026-07-17 · Os experimentos de resolução: onde o pixel vale e onde ele é jogado fora (medido, não teorizado).** Pergunta do ideador: "128px para tudo, inclusive árvores, ficaria bonito?" — respondida com DOIS experimentos, house style "só sabemos testando". **(A) Textura**: a MESMA árvore autorada em 16/64/128px (`author-tree-res.cjs`, formas normalizadas idênticas, detalhe escalando) renderizada pelo preview in-engine: **16→64 melhora visivelmente a árvore de PERTO; 64→128 não muda quase nada** — o buffer 320×180 descarta o excedente; ao longe nem 16→64 aparece. Teto de textura no render atual ≈ **64px para objetos de perto**; 128px-pra-tudo = pagar 64× por detalhe invisível. **(B) Render interno**: nova alavanca **`?res=LARGxALT`** no cliente (única mudança: W/H parametrizados; default 320×180 intocado) + bancada **`res-bench.mjs`** (FPS real contando rAF por 3s + recortes da MESMA região; CPU÷4 como proxy de celular). Medido na VM: **320×180 = 56fps** (÷4: 10.8) · **640×360 = 18.5** (÷4: 3.7) · **1280×720 = 4.4** (÷4: 0.7) · **1920×1080 = 2fps** (÷4: 0.4). Custo escala ~linear com a contagem de pixels — raycaster por software não tem almoço grátis. E o ganho visual SATURA: acima de 640×360 as texturas 16px viram o gargalo (A e B se encontram: subir render sem subir textura ganha borda, não informação). **Números absolutos são pessimistas** (VM fraca; máquina/celular reais rodam o 320×180 a 60) — as RAZÕES são o achado. A alavanca foi publicada: qualquer um pode sentir `?res=640x360` no próprio aparelho. **Recomendação do coder ao ideador (decisão de produto aberta): resolução POR PAPEL** — Habitantes-heróis 64px, estruturas 32–64, cenário 16–32, render interno 320×180 mantido (identidade + orçamento mobile); "128 pra tudo" descartado pelos dados. Rascunhos da brasa-pessoa 16px e 64px (D-42→) aguardam a mesma decisão de estilo. **Decidido pelo ideador em seguida: (a) Habitantes em 64px "pessoa do ofício" — aprovado; (b) o alvo passa a ser 640×360 rodando LISO** (ele sentiu engasgos no aparelho dele) → vira o D-45.
- **D-45 · 2026-07-17 · Renderização em camadas: 640×360 ~2× mais rápido sem perder o visual (medido por fase).** Pedido do ideador ("360 está ótimo, mas precisaria rodar liso"). Método house style: **perfilar antes de otimizar** — sondas por fase no render (`window.__nosPerf`: céu/chão/paredes/billboards/resto, 5 `performance.now()` por quadro, custo nulo), lidas pela `res-bench`. O perfil revelou: **o CHÃO era o vilão em qualquer resolução** (~10ms já no 320×180 — campos+água+GI+glow por pixel), e o glow re-amostrava a veia que o próprio loop acabara de calcular. Dois golpes: **(1) blocos RS×RS no céu e no chão** — céu e chão são conteúdo MACIO (gradiente, grama, água); em ?res= alto eles agora renderizam SEMPRE na densidade do 320×180 (RS=round(W/320): 2 no 640, 4 no 1280, 6 no 1920) e cada bloco replica o pixel — invisível na prática (antes/depois lado a lado idênticos), enquanto paredes, billboards e sprites ficam na resolução cheia (a nitidez fica onde o olho percebe — "resolução por papel" aplicada ao próprio framebuffer); RS=1 no 320×180 = caminho antigo byte a byte; **(2) glow inline no chão** reusando a veia amostrada (o `glowAt` de fora segue para paredes/billboards). Resultado medido (VM): **640×360: 18.5 → ~34-39fps (~2×)** · 1280×720: 4.4 → ~14.6 · 1920×1080: 2 → ~9.1; o custo restante em alta-res é só parede+billboard (a nitidez comprada). 320×180 segue igual em desempenho e "largo-noite" (veias/GI/chafariz — o teste ácido do glow) visualmente intacto; 368/368. **Parte 2 (ideador ainda sentiu engasgo "às vezes"): o perfil por FASE apontou o vilão real do stutter** — billboards. No carreiro (fileiras de árvore) o `bill` saltava de ~6ms (cidade) pra **17-19ms no 640×360** só de virar pra mata: 102 quadros lentos (>22ms) em 3s. Dois golpes: **(1) vegetação (arvore/tufo/flor/arbusto/nevoa) em blocos RS** — o `shade()` sai UMA vez por bloco e replica nas colunas vizinhas (oclusão pré-computada por coluna), heróis/prédios/portal/NPC/ilha ficam nítidos (step 1); no 320×180 (RS=1) é step 1 pra todos = idêntico. **(2) fim das alocações por quadro no sort dos billboards** — antes criava ~200 objetos `{b,d}`/quadro (pressão de GC = engasgo); agora arrays de rascunho REUSADOS e a distância mora em `b._d`, sem concat/slice. Resultado (VM, carreiro): **640×360 engasgos 102 → 12 (‑88%), fps 33 → 43**; e o **320×180 também ficou mais liso** (engasgos 9 → 3) — o fim do GC ajuda em toda resolução. Antes/depois idênticos no olho (mata em bloco lê como mata; a chegada mantém arco/chafariz/portal nítidos); 368/368. **Parte 3 (pedido: "ataca de novo, pros níveis do menu"):** faltavam LINHAS — a vegetação bloqueava só colunas (o texel/shade rodava por linha cheia) e o céu idem; e a pedra-3D (3 fbm por pixel, o pixel mais caro do jogo) era a reserva anunciada. Agora: **vegetação e pedra em blocos RS×RS de verdade** (colunas E linhas, sample uma vez por bloco, réplica respeita oclusão por coluna), **céu com linhas em bloco**, e a **fumaça** entrou no time dos macios. Medido nos 4 presets do menu (VM, mata): **Baixo 320 = 56.8fps · Médio 480 = 57.1 · Alto 640 = 52.4 (era 42.6; no início do D-45, 18.5) · Ultra 960 = 35.3**. Surpresa útil: no CPU÷4 (proxy de celular fraco) o **Médio roda MAIS liso que o Baixo** (15.6 vs 13.8fps) — RS=2 no 480 amostra o chão a 240×135, mais barato que 320×180, com paredes mais nítidas. Visual: mata idêntica em 640/960, fumaça normal, 320 byte a byte; 368/368.
- **D-47 · 2026-07-17 · O joystick cravado (2 buracos de pointer capture) + terceira rodada de otimização.** Bug reportado pelo ideador: joystick do celular "ficando cravado numa direção". Diagnóstico — DOIS buracos no `bindStick`: **(1) `setPointerCapture` pode FALHAR** (toque que já morreu quando a captura é pedida — Android apressado); sem captura, o `pointerup` que acontece fora do círculo nunca chega no elemento, e a rede global de `pointerup` limpava os pads antigos **mas não o `JOY`** → eixo preso até trocar de aba; **(2) segundo dedo ROUBAVA o stick** (qualquer `pointerdown` sobrescrevia o `pid` — o up do dedo original não batia mais). Fix: 1º dedo é o DONO (`if (pid !== null) return`), `setPointerCapture` em try/catch, e **rede global**: `pointerup`/`pointercancel` na janela zeram o stick cujo `pid` bate (`STICKS` registry) — o eixo NUNCA sobrevive ao dedo. Provado com eventos sintéticos: up-fora-do-círculo zera (antes ficava 0.72), dedo invasor não rouba e o dono segue mandando. **Otimização (3ª rodada):** código morto no chão (`world.at` POR PIXEL cujo resultado ninguém lia + `stepX`/`waterFrame` órfãos — sobras de refactors) e **minimapa a 30fps** (quadro sim, quadro não — imperceptível, ~0.4ms de volta em todo preset). Medido (VM, mata): **Alto 640 52.4→58.7fps (pior-quadro 40→25ms), Ultra 960 35.3→42.4 (engasgos 97→22), Médio 480 59.5 (1 engasgo em 3s), Baixo no teto da VM**. Lição em dobro: input de toque precisa de rede de segurança FORA do elemento (captura é promessa, não garantia); e refactor que não cata o próprio lixo deixa custo por pixel esquecido — grep de variáveis órfãs nos hot loops vale a pena periodicamente.
- **D-48 · 2026-07-17 · O joystick que "buga depois de um tempo": o guarda do D-47 criou uma ratoeira — corrigido com defesa em 4 camadas.** Ideador reportou: "de início funciona, depois só anda pra frente com a câmera derivando na diagonal". Diagnóstico com autocrítica: **o D-47 consertou um buraco e cavou outro** — o guarda "2º dedo não rouba" (`if (pid !== null) return`) assumia que TODO dedo gera um evento de soltura; quando o navegador engole um up/cancel (gesto do sistema, Android), o `pid` vira FANTASMA: os eixos ficam congelados no último valor (por isso "anda pra frente sozinho") e o guarda passa a **bloquear todo toque novo pra sempre** — bricka até recarregar. Fix: **não confiar em NENHUM evento específico** — (1) **`ACTIVE_PTRS`**: conjunto dos ponteiros vivos mantido em fase de CAPTURA na janela (nenhum stopPropagation esconde); (2) o guarda agora só protege **dono VIVO** (`ACTIVE_PTRS.has(pid)`) — dono fantasma é DESTRONADO na hora pelo toque novo; (3) **watchdog por quadro** no loop (`s.guard()`): dono sem ponteiro vivo = reset imediato, mesmo sem nenhum toque novo; (4) **rede do canal touch**: `touchend`/`touchcancel` com `touches.length === 0` zera tudo — touch events são um canal SEPARADO dos pointer events, então este cinto segura mesmo quando o outro rasga. `clearInputs` também re-centra o botão e solta o dono (o pause deixava o knob deslocado). Provado com eventos sintéticos nos 4 cenários (fantasma destronado; watchdog zera em 1 quadro; zero-dedos nuclea; dono vivo protegido). Lição que fica: **em input de toque, cada camada de proteção deve assumir que as outras falham** — eventos de soltura são promessa, não garantia; a única verdade estável é "que ponteiros estão vivos AGORA".
- **D-49 · 2026-07-17 · Billboards ORIENTADOS (arquitetura ancorada no mundo) + troca de preset sem teleporte + blindagem de gesto no celular.** Três achados do ideador numa rodada. **(a) "Os objetos ficam me encarando"** (chafariz, arco, portal): billboard clássico encara a câmera sempre — certo pra árvore, ERRADO pra arquitetura. Novo recurso no renderizador: **`b.orient`** — o sprite vira um SEGMENTO fixo no mundo (projeta as 2 pontas, interpola `1/ty` e `u/ty` por coluna = perspectiva correta, clip no near, oclusão por coluna via zbuf/wallTop) e preenche como parede com transparência. **Arco de entrada** (plano ⊥ carreiro, mesma linha dos pilares) e **Portal do Átrio** (plano N-S) ancorados: circundar agora ENCURTA o objeto até o perfil, como parede de verdade. **O chafariz fica encarando de propósito**: é sólido de revolução (igual de todo lado) — câmera-facing é a emulação correta; plano fixo o faria sumir de perfil. **(b) Trocar gráfico teleportava pro spawn e voltava pro dia**: a recarga do preset agora salva `nos_resume` (posição+ângulo+hora) e o boot consome one-shot — a hora volta via `timeOffset` (o relógio SEGUE), não `todOverride` (congelaria); `?cam`/`?tod` da bancada mantêm prioridade. Verificado: teleportou→trocou pra Alto→voltou exato em 640×360. **(c) Joystick pós-troca**: além do D-48, blindagem de GESTO — `touch-action:none` + `overscroll-behavior:none` no `#stage` (pull-to-refresh/pinch não roubam mais o dedo) e sticks subidos de 6%→8% (fora da zona do gesto de "home", suspeito do up engolido "depois de um tempo"). Nota honesta: preset pesado em aparelho fraco = main thread saturada = input com lag — se o joystick "pesar" no Ultra, é fps, não bug; o menu já recomenda Baixo/Médio no celular. (celular tem controle agora) — e por que NÃO existem "3 artes".** Pedido do ideador: botão de pausa no celular com opções de gráfico (resolução) e a dúvida "ou pesaria ter 3 artes (16/32/64px)?". **Resposta de produto (fundamentada no D-44):** não faça 3 artes — textura mais densa que o render é jogada fora no downsample, então "16/32/64 como opção do jogador" custaria o triplo e a maioria não veria diferença; e um sprite de herói 64px **já serve em qualquer resolução** (só mostra mais detalhe de perto). O que o jogador quer por "gráfico baixo/médio/alto" é a **resolução INTERNA do render** (o `?res=`) — uma alavanca só, zero arte duplicada. **Implementação:** botão ⏸ (`#pauseBtn`) visível só em `pointer: coarse` (no PC o ESC já pausa) que abre o `#menu`; o menu ganhou uma seção de gráficos — presets **Baixo 320×180 · Médio 480×270 · Alto 640×360 · Ultra 960×540** + toggles de **luz (GI)** e **som** como botões (no celular não há tecla G/V). O preset salva em `localStorage nos_res` e **recarrega limpo** (`location.pathname`) — W/H são `const` no boot (mudar de verdade em runtime exigiria re-alocar dezenas de buffers derivados de W/H; recarregar é honesto e robusto); o boot lê `?res=` (override da bancada) > `nos_res` (menu) > 320. `toggleGi` extraído pra ser compartilhado por tecla e botão; `refreshMenu()` reflete o preset/toggles ativos ao abrir. Verificado com Pixel 5 emulado (⏸ visível, menu abre, "Baixo" ativo por padrão; salvar 640×360 e recarregar aplica 640×360 com "Alto" aceso); 368/368.
- **D-50 · 2026-07-17 · Profundidade nos arcos (extrusão em fatias) + A PRANCHETA (câmera de topo) + skill /estruturas.** Três pedidos do ideador, resumidos: (a) **arco e portal com faces 3D**: `b.depth` no billboard orientado — extrusão em FATIAS ao longo da normal, trás→frente, com AO sutil (0.82→1.0); fatias ADAPTATIVAS à distância (passo ≤1.2px de tela no rasante — K fixo virava "pente" de perto, achado da 1ª iteração; AO 0.66 pesava, achado da 2ª). Pilares agora têm face lateral, verga tem espessura, véu do portal em camadas. (b) **A PRANCHETA** (`npm run prancheta`): planta técnica de qualquer recorte lida da fonte VIVA — novo gancho `window.__nosMapa()` expõe colisões (troncos exatos), paredes+alturas, billboards e planos orientados direto das estruturas do cliente (zero duplicação, ao contrário do bake-gi); desenha grade, hachura de tile sólido, círculos de colisão, traços dos planos. É a "câmera de topo pra auditar colisão e criar estruturas" pedida. (c) **skill `/estruturas`** (`.claude/skills/estruturas/`): formaliza o ciclo prancheta→construir→prancheta→olhar 3 ângulos, com a tabela das peças (cityWall/orient/depth/addTrunk) e as regras (baker sync, paleta). Docs atualizadas: RECURSOS (prancheta, res-bench), CODER (seção Otimização + Estruturas). **Próximo passo planejado (F17): formato PLANTA** — blueprint declarativo (paredes/colisões/billboards num objeto) que o motor "sobe" sozinho; a prancheta é a metade de leitura desse ciclo.
- **D-51 · 2026-07-17 · PLANTA v1 provada: o Santuário do Pulso nasceu de um blueprint.** Pedido do ideador ("podemos testar a geração de estruturas"). Implementado `plantar(planta)` no cliente: uma estrutura inteira é um OBJETO declarativo — `paredes` (tile/kind/altura), `colisoes` (círculos finos), `billboards` (com orient/depth) — e o motor sobe tudo com as primitivas de sempre; registro em `PLANTAS`. **Prova real:** o Santuário do Pulso, capela aberta ao sul do largo — 5 paredes de pedra em U (fundo 0.95, flancos 0.55), 2 lampiões na boca, relicário central: um mini-Núcleo (o sprite canônico, escala 0.42) pulsando violeta sobre pedestal com colisão. **O ciclo da skill /estruturas funcionou e PEGOU um erro meu:** a 1ª posição tapava a entrada com uma árvore (círculo em 46.6,20.2 visível na prancheta que eu li mal) — deslocado +1 tile a leste, as duas árvores viraram MOLDURA da entrada. Baker sincronizado (5 caixas + 3 luzes: relicário violeta 0.15/0.5/1.0 + lampiões) e re-assado; noite verificada: o relicário brilha dentro da capela. Refinos anotados p/ PLANTA v2: variante de parede SEM janela (a janelinha da textura stone destoa em muro de santuário), `limpar` (raio que remove scatter — tufos dentro da capela), exportar caixas/luzes pro baker automaticamente (matar a duplicação de vez), e `origem`+rotação pra reusar plantas.
- **D-52 · 2026-07-17 · Lar unificado das ferramentas: `tools/` + índice.** Pedido do ideador (organizar as ferramentas numa pasta só, bem documentada) antes do trabalho grande do motor de segmentos (caminho A escolhido). As bancadas de auditoria (`olhar`/`ouvir`/`prancheta`/`res-bench` + `pontos.json`) saíram de `prototipos/fps/qa/` pra **`tools/bancadas/`**, ao lado do `tools/art-mcp/` (arte). Novo **`tools/README.md`** é o índice mestre — e, com honestidade, lista as ferramentas que ficam FORA de propósito (pipeline de sprites em `assets/tools/` = acoplada ao CI; `build-fps` em `site/scripts/` = gatilho de deploy; `bake-gi` em `prototipos/fps/bake/` = caminho fixo no build) com o motivo de cada uma, pra nunca virar caça ao tesouro. Regra nova: bancada de coder nasce em `tools/bancadas/`. Refs atualizadas (package.json, .gitignore, RECURSOS, CODER, skill /estruturas); histórico (DECISIONS/CONTINUITY antigos) preservado como log. 368/368; sem mudança no cliente (sem deploy).
- **D-53 · 2026-07-17 · O motor de SETOR/PORTAL: a primeira casa de verdade, com interior.** O ideador redirecionou ("você está criando estruturas no estilo wolfstein, eu queria algo mais realístico, casas bem formadas, com interior") e mandou ser crítico ("você deve ser crítico e não aceitar resultado ruim"). O DDA por tile é classe-Wolfenstein; casa realística pede classe-Doom/Build. Escolhido o **caminho A** e construído o núcleo do renderizador de segmentos: **parede = SEGMENTO fino** em qualquer ângulo, com `b` (altura do PÉ — >0 vira VERGA/abertura por baixo) e **topo inclinável** (`ta`→`tb`, que forma a EMPENA). Cada coluna coleta TODOS os hits raio×segmento (o `t` já é a distância PERPENDICULAR, pois `rd = dir+plane*camX`, então compara maçã-com-maçã com o DDA), ordena longe→perto e pinta a faixa de cada um sobre o fundo — **compositor de portal**: a porta é a verga com vão livre embaixo, o raio passa e enxerga o interior. **Telhado** = passo à parte (roof-cast por coluna: slab do AABB acha o trecho, marcha e projeta as duas águas; fecha o topo por fora E vira o forro por dentro). A casa-protótipo (cabana de duas águas no descampado oeste, 38–41×12–15, porta ao sul) tem soco de pedra, reboco, meia-madeira, 2 janelas acesas, piso de tábuas e cume N–S. Colisão por distância ponto-segmento (verga/empena acima da cintura não colidem: passa-se por baixo). **Auditado em 6 ângulos, e o "seja crítico" pegou 3 defeitos reais, todos corrigidos:** (1) telhado virava LEQUE radial visto do cume end-on — as fiadas alternadas 50/50 aliavam; troquei por gradiente liso (beiral na sombra→cume claro) + passo fino. (2) verga sem oclusão deixava árvore atrás VAZAR por cima da porta vista de dentro (a porta "voltava" a ser vão inteiro) — o modelo de billboard só oculta PRA BAIXO (`wallTop`); adicionei um **oclusor SUPERIOR** (`occHiZ`/`occHiY`+`OCCH`) que esconde o sprite atrás por CIMA da abertura, sem tocar no `wallTop` (senão o forro do teto some visto da porta — erro da 1ª tentativa, `nearAny`, revertido). (3) proporções de fortaleza → beiral 1.02, cume 1.66, porta 0.72. 368/368; chafariz (cena densa) sem regressão; 48–58fps nos 4 presets (roof-cast só marcha colunas que cruzam a pegada). Pontos canônicos `casa`/`casa-porta`/`casa-dentro` no `pontos.json`. **Limites honestos (próximos passos do motor de setor):** sprite ATRÁS de abertura é ocluído, não visto através (z single-depth por coluna — NPC dentro de casa visto pela porta pede z por-pixel); a prancheta/`__nosMapa` ainda não enxerga os SEGS; a casa não está no bake de GI (interior usa fog+brilho de janela, sem bounce); janela não derrama luz no chão. Nada disso é bloqueante pra provar que dá pra fazer CASAS DE VERDADE — e dá.
- **D-54 · 2026-07-17 · GPU no cliente: WebGL provado, beleza iterada com o ideador.** "Pixel art está lindo, só que rodando terrivelmente ruim... me ajude a usar GPU". Diagnóstico: rasterizamos cada pixel na CPU e o custo cresce com a tela; na GPU renderiza-se num framebuffer FIXO (?res) com upscale NEAREST — custo independente da tela. Protótipos publicados sem tocar o cliente: `/fps/gpu.html` (a casa D-53 por geometria, pixel art puro) e `/fps/gpu-beauty.html` (teto de beleza: SOMBRA direcional por shadow-map RGBA-packed+PCF, luz de céu hemisférica, texturas 64px, partículas). Iterações guiadas pelo ideador com referência visual: toras VERTICAIS castanho-mel (b/c/d/e/f — sem veio, uniformes, "cor de árvore"; a Resurrect64 não tem castanho quente → **madeira em tons RGB reais fora da paleta, aprovada** — dither compunha mas ficava hachurado), janelas = ABERTURAS reais (panelHole: vão na malha + moldura + cruzeta + DUAS ABAS venezianas), porta estreita/alta com batente, telha de barro capa-canal, empena de toras (a tesoura pintada saiu — chapada; treliça 3D anotada como próximo passo). Pegas técnicas: WebGL1 exige POT p/ REPEAT (16×24 amostrava preto → 16×32); swiftshader ≠ medida de celular (o fps que vale é o do aparelho).
- **D-55 · 2026-07-17 · Rumo v2/v3 + A OFICINA (ambiente padrão de criação).** Decisões do ideador: (a) branch **`v2`** criada preservando o jogo atual (CPU raycaster) — rollback/manutenção; a **`main` desenvolve o v3 (GPU)**, publicado em paralelo em `/fps/v3/` até a paridade (o `/fps/` v2 segue no ar durante o port). (b) **A OFICINA**: espaço isolado e PADRONIZADO pra criar objetos/texturas/animações (sons/reflexos na fila) em `prototipos/fps/v3/` — `motor/` (mat4/tex/geo/render: sol+sombra PCF, luz de céu, névoa, partículas, grama, ?res) + `pecas/` (contrato: `meta` + `construir(ctx) -> {lotes, animar?}`; animação por matriz/lote) + `visor.html` (?peca=nome). REGRA DE OURO: o visor usa o MESMO motor que o cliente v3 — o que fica bom na oficina fica bom no jogo. Peças: `casa-toras` (migrada do gpu-beauty, idêntica) e `_modelo` (template: cubo texturizado girando/flutuando — prova o contrato inteiro). Bancada `npm run peca -- <nome>` (olhar-peca.mjs: server estático p/ ES modules + 3 ângulos). Limites honestos no README do v3: sem som/reflexo/câmera andável no visor ainda; o port do mundo é o trabalho que segue.
- **D-56 · 2026-07-17 · O plano da potência: objetivo final + arsenal de ferramentas (docs/FERRAMENTAS.md).** Pedido do ideador ("ferramentas a níveis alienígenas" + "precisamos de um objetivo final"). Varredura por 5 agentes paralelos sobre o arsenal real (bancadas, arte, motor v3, som, fluxo) → ~60 propostas, sintetizadas em ~30 itens com prioridade. **Objetivo final: "da frase ao mundo no ar em uma sessão — sem passo cego e sem regressão"** (a Oficina vira A Fábrica — lore aplicada ao próprio desenvolvimento). Potência definida em 4 eixos: ciclo em segundos, visão total, alavancagem 10×, gates automáticos. Ordem de construção travada: (1) porteiro-visual+diff, (2) bancada-viva+contato+filme, (3) porta-mundo+cena+câmera, (4) perf-gpu+orçamento+paridade, (5) materiais+paleta-estendida. O plano vive em docs/FERRAMENTAS.md; cada item vira rodada na vez dele.
- **D-57 · 2026-07-18 · O gargalo é o briefing, não a ferramenta + escopo do v3 travado (linguagem + prune).** Debate curto com o ideador que reposicionou tudo. **(0) A virada de chave:** ele intuía uma ferramenta que "automatizasse muito" meu trabalho e não achava a palavra; a palavra que ele mesmo achou foi **"nós combinarmos informações"**. Conclusão aceita pelos dois: **o gargalo nunca foram minhas mãos (as ferramentas) — é a informação/briefing entre nós.** Informação rica + ferramenta mediana > ferramenta divina + informação pobre (é o que o MCP-pro-Blender dele ensina na marra: MCP tira teto, não esculpe; "bom pra IA" ≈ bom pra artesão humano — brief claro, referência, restrição, jeito de ver). **Metade do trato é minha:** eu parava de pensar no 1º substantivo (disse "casa" → fabriquei cabana de toras por 6 rodadas sem NUNCA perguntar *que arquitetura o NÓS tem* — a cabana caiu direto no furo de informação). **Trato firmado:** ele fala mais claro; **eu extraio o brief ANTES de construir qualquer coisa (nem que seja 3 perguntas), não corro com o palpite.** E respostas mais diretas a partir daqui. **(a) Linguagem das estruturas do NÓS = sci-fi decaído: concreto tomado por mato, ruína de tecnologia antiga sobre a natureza.** A natureza cartoon da v2 já tinha estilo; as estruturas eram "bloco de Wolfenstein" porque ninguém nunca definiu o idioma delas — agora tem. Toda estrutura v3 mira esse alvo (a cabana de toras foi teste de motor, NÃO é o tema). **(b) Prune de escopo do v3** (o ideador: "fui ambicioso pensando que teriam mais pessoas e mais mundos além de mim"). Como o v3 é port do zero, "remover" é limpo: **não apago nada do v2 (fica congelado como registro) — só NÃO porto.** FICA: mundo explorável/construível + **economia** (é a única coisa transferível entre mundos, então o encanamento dela fica junto — **₱**, **ledger** = arquivo de saldos+transações que torna o ₱ real, **identidade** = conta GitHub do jogador = "nome de personagem", o outro transferível). SAI do v3: outros NPCs, criaturas no mapa (animais entram DEPOIS, não agora), bancadas de craft no mundo, e a **narrativa** (história do Coração, ficção de federação/muitos-mundos) — cortar narrativa ≠ cortar o encanamento econômico. **(c) Um único NPC: o mago-guia.** Jogador conversa pra tirar dúvida; a gente "dá conhecimento" a ele. Implementação decidida: **base de conhecimento git-native (grátis, sem cota, determinística)** — pergunta via issue → Action busca/responde; cobre "como jogo?/onde fica X?/lore". LLM ao vivo (GitHub Models, grátis-com-limite, chamável de Action) fica como fallback OPCIONAL futuro — **não dependemos de cota**. Esclarecido pro ideador: **Copilot é assistente de código (pago, no editor), não serve de NPC; e "a IA que usamos" são DUAS** — eu (Claude), o construtor, rodo fora do jogo; a IA do mago seria outra, em runtime. **(d) IA ambiente falando de tempos em tempos: documentar que é possível + deixar exemplo, mas NÃO ligar** (só o mago). Escopo do v3, martelo batido: **mundo sci-fi decaído + mago-guia + economia (₱/ledger/identidade); sem outros NPCs, sem criaturas, sem craft, sem narrativa.**
- **D-58 · 2026-07-18 · O chão v3 nasce (ilha-chao) + o bug do hash2 — primeira vitória do senso crítico [cpu].** Port do chão decidido em debate: **só chão por ora**; ilha flutuante mantida; **sem carreiro**; a borda da v2 "parecia margem de rio" → o conserto é a ilha ser um SÓLIDO com queda visível (vão de ar até o mar de nuvens = altura); **sem a parte de baixo** (pedido: o jogador nunca a vê — fica só uma fita de terra de 2.4u pra não ler papel); ilha NA ESCALA V2 (grade 64×64 → R=28, ~56u de diâmetro). Peça `pecas/ilha-chao.js`: grama 64px/unidade (receita genGrassTile: manchas+pinceladas+touceiras+flores, acalmada pra não virar confete a distância), lago com PRAIA de areia, fita de terra com normal inclinada pro céu (não vira breu na sombra), mar de nuvens 4 tons. Motor ganhou opções de PAISAGEM no contrato: `palco:false` (a peça É o chão), `particulas:false` (pólen em paisagem lia como enxame), `fog:[início,alcance]` e `far` próprios, `camera:{e,r}` sugerida (a bancada só passa e/r quando pedidos). **O BUG DOURADO: `hash2` do tex.js usava `>>` (shift com sinal) + multiply estourando float — TODO ruído do v3 saía espremido em [0,0.5)** (nuvem 100% numa cor só, dither pobre — provável causa do "dither hachurado" da madeira em D-54f). Achado MEDINDO, não olhando: histograma da textura em Node (100% cor 46) → estatística do fbm (máx 0.44) → fix com `Math.imul` + `>>>` → faixa cheia [0.11,0.86]. É a tese do D-56 provada: o screenshot enganava ("deve ser o fog"), o número não. Segunda pegada medida: o fog padrão ESMAGAVA o mar de nuvens (A/B com fog recuado provou); terceira: nuvem com repeat de 73u caía inteira dentro de um blob (→24u). No ar em `/fps/v3/visor.html?peca=ilha-chao`. Limites honestos: água/nuvem paradas (animação e reflexo virão), grama ainda meio salpicada ao longe, pontinhos pretos de aliasing na borda distante.
- **D-59 · 2026-07-18 · Port das ÁRVORES da V2 pro v3 (growTree extraído fiel) + 2 consertos de motor.** O ideador: "vamos portar árvores, pega uma imagem da V2 como base". Base = `carreiro` (npm run olhar): copa fofa em camadas (sombra teal→topo amarelo-verde, estilo BotW), contorno de tinta, tronco com casca+raiz, franja de grama. Port: o `growTree` (L-system, ~415 linhas) + dependências (SPECIES/ramps, mulberry32, **hash2/vnoise DA V2**, BAYER, clamp) **extraídos LITERALMENTE** de nos-fps.html pra `motor/arvore.js` via script (scratchpad/extrai-arvore.mjs), NÃO redigitados. Decisão-chave: manter a hash2 buggy da v2 (meia-faixa) LOCAL ao módulo — o gerador foi afinado com ela; a hash2 nova (D-58) mudaria a distribuição e fugiria da referência. Peça `arvore.js`: cada árvore = **cruz de 2 billboards** perpendiculares (volume de qualquer ângulo ao andar em volta, não gira chapada), normal pra CIMA (luz uniforme preserva o sombreado BAKED do sprite). 3 espécies provadas (carvalho/florida/bétula), paletas certas (cerejeira rosa, etc.). **2 bugs de motor consertados no caminho:** (1) **sombra retangular** — o passe de profundidade ignorava alfa, então sprite recortado jogava sombra de CAIXA; agora o depthProg amostra a textura e faz `discard` no alfa (cutout shadows pra toda folhagem futura). (2) **textura PRETA** — árvore é 166×218 = NPOT, e WebGL1 com REPEAT em NPOT rende preto; `glTex` agora auto-CLAMP em NPOT (billboard usa UV 0..1, não precisa REPEAT). No ar em `/fps/v3/visor.html?peca=arvore`. Próximos: frames de vento (growTree aceita windAmt -1/0/1), plantar árvores na ilha-chao, LOD/impostor pra floresta densa.
- **D-60 · 2026-07-18 · Ferramentas de senso crítico [cpu] CONSTRUÍDAS e MEDIDAS por benchmark (o pedido: "descobrir quais ajudam, iterar, listar o que não ajuda").** Aplicação da tese do D-56 às próprias ferramentas: "será que ajuda?" virou NÚMERO. **Método:** benchmark = peças reais (arvore3d, ilha-chao, casa-toras) × defeitos PLANTADOS (18 mutações em 5 domínios) + controles limpos → cada ferramenta pontuada por pegar o defeito do seu domínio (recall) sem alarme falso em limpo/outro-domínio (precision) = F1. **Base técnica-chave:** `sandbox.mjs` roda o `construir()` das peças em NODE PURO (canvas-stub cobre texCanvas/bufToCanvas) — malhas + pixels de textura sem browser, em ms, determinístico e paralelizável. **Construção:** lint-de-malha + porteiro inline (eu); as 4 de textura (distancia-paleta, detector-de-seam, detector-de-banding, contador-de-pixels-orfaos) por WORKFLOW de 4 agentes em worktrees isolados, cada um iterando contra o benchmark. **Resultado — todas gateiam o defeito REAL: NÚCLEO F1=1.00, ZERO falso-alarme nas 4 peças de arte de verdade.** distancia-paleta usa CIEDE2000 puro + allowlist da madeira aprovada (D-54f, lida de casa-toras.js); detector-de-seam distingue costura injetada (borda chapada + salto) de textura-que-só-não-ladrilha (billboard/água) sem metadado; etc. **Round ADVERSARIAL (versão sutil de cada defeito) revelou o PISO honesto:** seam/banding perdem o sutil (0/3), paleta perde desvio de 1-tom (a fronteira paleta↔banding numa faixa chapada off-palette é REAL — forçar a paleta a pegar isso a faz morder o banding; documentado como piso, não re-tunado), órfãos dá 1 FP sob ruído pesado. lint-de-malha é exato (—). **Nenhuma foi dud** → a "listinha do que não ajuda" virou "limites honestos por ferramenta" (onde NÃO confiar). **Empacotado (2º pedido):** `npm run auditar <peca>` (gate [cpu] numa peça real, exit≠0 em achado) + `npm run porteiro <peca>` (gate de render: pageerror/__ready/frame degenerado, decoder PNG próprio via zlib) + `npm run bench` (placar núcleo/adversarial). Viraram a **skill `/auditar-peca`** (`.claude/skills/`) com a tabela de pisos — o senso crítico entra no meu loop antes de commitar peça, com a regra "todo julgamento cita ≥1 número". Prova viva da tese: um `[cpu]` (não o olho) pegou o bug do `hash2` no D-58.
