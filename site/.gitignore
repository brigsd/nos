node_modules/
dist/

# Regenerated on every dev/build by scripts/copy-data.mjs from the repo's
# canonical world/, worlds/ (R6, D-17) and assets/sprites/ - never the
# source of truth, never committed.
public/world/
public/worlds/
public/assets/

# Regenerated by scripts/build-fps.mjs from prototipos/fps/ + world/heart.json.
public/fps/

# Evidência de QA (screenshots Playwright) — regenerada pelos scripts qa/*.mjs,
# fica só no histórico/branches de trabalho, nunca na main.
qa/*.png
